name: CI

on:
  push:
    branches: [master, main]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/tore?schema=public
      AUTH_SECRET: ci-auth-secret-must-be-at-least-32-chars
      AUTH_URL: http://localhost:3000
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      NODE_ENV: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Unit tests
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Production build
        env:
          NODE_ENV: production
          # Explicit MVP deployment allow-flags for CI (non-secrets).
          # Real multi-tenant production must omit these and set Redis/S3/email/https.
          TORE_ALLOW_LOCAL_STORAGE: "1"
          TORE_ALLOW_NO_REDIS: "1"
          TORE_ALLOW_NO_EMAIL: "1"
          TORE_ALLOW_INSECURE_URLS: "1"
          FILE_STORAGE: local
          EMAIL_PROVIDER: console
        run: npm run build
